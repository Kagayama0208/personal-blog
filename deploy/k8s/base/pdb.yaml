# Keep at least one app pod serving during voluntary disruptions (node drains).
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: payload-blog
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: payload-blog
      app.kubernetes.io/component: web
